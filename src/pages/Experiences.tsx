import React from "react";

type Props = React.HTMLAttributes<HTMLDivElement>;

const Experiences: React.FC<Props> = (props) => {
  return <div {...props}>Experiences</div>;
};

export default Experiences;
